// 2.	Sort a list of words based on their lengths.
// o	Define an array of words.
// o	Adapt the bubble sort algorithm to compare word lengths instead of numerical values.
// o	Print the sorted list of words, arranged from shortest to longest.

#include <iostream>
#include <string>
#include <iomanip>

using namespace std;

int main()
{
    string arr[]={
        "good", "fellow", "well", "potenial", "jump", "phone", "Computer"
    };
    int size =  sizeof (arr) / sizeof(arr[0]);

    for (int i = 0; i < size - 1; i++)
    {
        
    }
    
















    
	
    for (int i = 0; i < size; i++)
    {
        cout << arr[i] << " ";
    }

    return 0;
}